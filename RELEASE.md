# 发布检查清单

每次改完代码、准备发布新版本时，按顺序做：

1. **改 `public/sw.js` 里的 `CACHE_VERSION`**（最容易忘、也最关键的一步）
   - 例如 `"v1"` → `"v2"`
   - 不改的话，已安装用户的浏览器会因为 cache-first 缓存策略一直用旧版本，永远看不到更新
   - 判断标准：只要这次改动涉及任何会被缓存的文件（页面、组件、样式、图标、`wa-sqlite.wasm` 等），就要改

2. **`npm run build`**，确认无报错，产出新的 `out/` 目录

3. **把新的 `out/` 部署到托管平台**（覆盖旧版本）

4. 用户端：关闭再重新打开已安装的桌面应用（或刷新页面），即可自动获取新版本
   - 不需要用户手动卸载重装
   - 本地数据存在浏览器 OPFS，与应用外壳缓存分离，更新不会影响已有数据（错题本、自定义篇目等）
